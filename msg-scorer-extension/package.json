{
  "name": "msg-scorer-extension",
  "version": "1.0.0",
  "description": "MsgScore Chrome拡張 — Gmail・LINE OA Managerスコアリング",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "generate-icons": "node generate-icons.js",
    "zip": "npm run build && zip -r msg-scorer-extension.zip manifest.json icons/ dist/ -x \"*.DS_Store\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.323",
    "esbuild": "^0.24.0",
    "sharp": "^0.33.5",
    "typescript": "^5.0.0"
  }
}
