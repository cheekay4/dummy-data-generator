{
  "name": "palpation-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "data:download": "node packages/anatomy-data/scripts/download.mjs",
    "data:convert": "node packages/anatomy-data/scripts/convert.mjs"
  }
}
